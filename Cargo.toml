[package]
name = "arkived"
version = "0.0.1"
edition = "2021"
description = "A fast, Rust-native storage client for Microsoft Azure. Under active development."
license = "MIT"
repository = "https://github.com/arkived-app/arkived"
readme = "README.md"
keywords = ["azure", "storage", "cloud", "cli"]
categories = ["command-line-utilities", "filesystem"]

[lib]

[dependencies]
